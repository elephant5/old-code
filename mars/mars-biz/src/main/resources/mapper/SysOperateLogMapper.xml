<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.colourfulchina.mars.mapper.SysOperateLogMapper">

    <select id="dynamicSelectById" parameterType="com.colourfulchina.inf.base.entity.SysOperateLogInfo" resultType="map">
        select * from ${tableName}
        where ${tableId}=#{rowKey}
    </select>
</mapper>
